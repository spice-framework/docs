---
title: Integrations
description: Independently versioned Spice starters for persistence, caching, messaging, transport, identity, observability, and mail.
---

import EcosystemMap from "@components/EcosystemMap.astro";

Spice starters are opt-in modules. Each repository owns its compatibility,
configuration, security defaults, lifecycle, and support policy.

<EcosystemMap />

Use the exact starter versions selected by the current ecosystem snapshot. The
portal does not imply that every independently versioned repository shares one
global release number.
